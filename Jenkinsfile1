pipeline {
    agent any
    
    tools {
        nodejs "node"
    }

    stages {
        stage('install') {
            sh 'npm install'
        }

        /* stage('test'){
            steps {
                dir('node') {
                    sh 'npm run test'
                }
            }
        }  */
    }
}